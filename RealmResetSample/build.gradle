buildscript {
  repositories {
    mavenCentral()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:1.2.3'
  }
}

allprojects {
  repositories {
    mavenCentral()
    jcenter()
  }
}

ext {
  minSdkVersion = 15
  compileSdkVersion = 22
  buildToolsVersion = '22.0.1'
}
